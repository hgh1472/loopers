feign:
  client:
    config:
      loopers-client:
        connectTimeout: 2000
        readTimeout: 800

      loopers-get-client:
        connectTimeout: 2000
        readTimeout: 300


---
spring:
  config:
    activate:
      on-profile: local, test

---
spring:
  config:
    activate:
      on-profile: dev

---
spring:
  config:
    activate:
      on-profile: qa

---
spring:
  config:
    activate:
      on-profile: prd

